services:
  easywords:
    source:
      type: dockerfile
      path: Dockerfile
    env:
      NODE_ENV: production
      DATABASE_URL: file:/data/easywords.db
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}
      TURSO_AUTH_TOKEN: ${TURSO_AUTH_TOKEN}
    volumes:
      - /data:/app/data
